import React, { Component } from 'react'
import spinner from './spinner.gif'
const Spinner =()=> {
        return (
            <React.Fragment>
                <img src={spinner} style={{width:'200px', display:'block'}}/>

            </React.Fragment>
        )
    
}

export default Spinner
